[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
# По умолчанию не запускать долгие интеграционные тесты (нужен OPENAI_API_KEY)
# Запуск всех тестов, включая integration: pytest -m ""
# Только быстрые: pytest -m "not integration"
addopts = -v -m "not integration"
